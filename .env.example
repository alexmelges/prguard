# GitHub App credentials
APP_ID=your_app_id
PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
WEBHOOK_SECRET=your_webhook_secret

# OpenAI API (server default — repos can override via .github/prguard.yml openai_api_key)
OPENAI_API_KEY=sk-...

# Database (optional, defaults to ./prguard.db)
DATABASE_PATH=./prguard.db

# Server
PORT=3000
NODE_ENV=production

# Logging level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info
